# ✅ قائمة المهام المتبقية لإكمال نظام SCADA

> **تاريخ الإنشاء:** 18 ديسمبر 2025
> **المصدر:** تحليل الفجوة بين ملف `05_نظام_المراقبة_والتحكم.md` والحالة الحالية للنظام.

## 🎯 ملخص الحالة الحالية

النظام الحالي يمتلك أساساً قوياً، حيث تم بناء هيكل قاعدة البيانات (`schema.prisma`) بشكل شبه كامل، وتم إصلاح واجهات SCADA الأساسية (لوحة التحكم، المحطات، التنبيهات). ومع ذلك، معظم وظائف Backend والواجهات المتقدمة لم يتم تنفيذها بعد.

---

## 📋 المهام المتبقية

### 1. Backend - إكمال وحدات API

#### 1.1. وحدة الطاقة (Energy - المهمة 25)
- [ ] **API:** `GET /api/v1/energy/summary` - لتوفير ملخصات الطاقة (يومية، شهرية).
- [ ] **API:** `GET /api/v1/energy/consumption` - لتحليل الاستهلاك حسب القطاع.
- [ ] **API:** `GET /api/v1/energy/forecast` - لتوقعات الطلب على الطاقة.
- [ ] **API:** `GET /api/v1/energy/losses` - لتحليل الفقد في الشبكة.
- [ ] **Service:** تنفيذ العمليات الحسابية لتجميع البيانات من `readings_history` وإنشاء `energy_summary`.

#### 1.2. وحدة الجودة والسلامة (Quality & Safety - المهمة 26)
- [ ] **API:** `GET /api/v1/quality/metrics` - لعرض مؤشرات جودة الطاقة (PF, THD, Unbalance).
- [ ] **API:** `GET /api/v1/reliability/metrics` - لحساب وعرض مؤشرات الموثوقية (SAIDI, SAIFI).
- [ ] **API:** `GET /api/v1/incidents` - لعرض الحوادث المسجلة.
- [ ] **API:** `POST /api/v1/incidents` - لتسجيل حادث جديد.
- [ ] **API:** `GET /api/v1/inspections` - لعرض فحوصات السلامة.
- [ ] **Service:** تنفيذ منطق حساب مؤشرات الجودة والموثوقية.

#### 1.3. وحدة الأمان والحماية (Security - المهمة 27)
- [ ] **API:** `GET /api/v1/security/cameras` - لعرض قائمة الكاميرات وحالاتها.
- [ ] **API:** `GET /api/v1/security/access-log` - لعرض سجل الدخول والخروج.
- [ ] **API:** `GET /api/v1/security/events` - لعرض أحداث الأمان (اقتحام، حركة).
- [ ] **API:** `POST /api/v1/security/events/:id/resolve` - لمعالجة حدث أمني.
- [ ] **Service:** ربط التنبيهات مع أحداث الأمان.

#### 1.4. وحدة التكاملات (Integrations - المهمة 28)
- [ ] **API:** `GET /api/v1/integrations/devices` - لعرض الأجهزة المتكاملة (Acrel, STS, IoT).
- [ ] **API:** `POST /api/v1/integrations/sync` - لبدء مزامنة البيانات يدوياً.
- [ ] **API:** `GET /api/v1/acrel/meters` - لجلب قائمة عدادات Acrel.
- [ ] **Service:** تنفيذ منطق التكامل الفعلي مع Acrel API (محاكاة حالياً).
- [ ] **Webhooks:** `POST /api/v1/acrel/webhooks/*` - لإنشاء Endpoints لاستقبال البيانات من Acrel.

#### 1.5. وحدة التوأم الرقمي (Digital Twin - GIS)
- [ ] **API:** `GET /api/v1/gis/network-map` - لتوفير بيانات خريطة الشبكة.
- [ ] **API:** `GET /api/v1/gis/nodes` - لإدارة عقد الشبكة (أعمدة، محولات).
- [ ] **API:** `GET /api/v1/gis/segments` - لإدارة مقاطع الشبكة (الكيابل).
- [ ] **Service:** تنفيذ وظائف تحليل الشبكة مثل `analyze_voltage_drop`.

---

### 2. Frontend - إكمال الواجهات

#### 2.1. الصفحات الرئيسية
- [ ] **صفحة الأجهزة (`/devices`):** إنشاء Component لعرض قائمة الأجهزة (`scada_devices`) مع إمكانية الفلترة والبحث.
- [ ] **صفحة الأوامر (`/commands`):** إنشاء Component لعرض سجل الأوامر (`control_commands`) وحالاتها.
- [ ] **صفحة الخريطة (`/map`):** إنشاء Component لعرض خريطة الشبكة التفاعلية باستخدام بيانات التوأم الرقمي.
- [ ] **صفحة التقارير (`/reports`):** إنشاء Component لعرض تقارير الطاقة والجودة.

#### 2.2. لوحات المراقبة المتقدمة
- [ ] **لوحة جودة الطاقة:** إنشاء واجهة متخصصة لعرض رسوم بيانية للتوافقيات (Harmonics) وعدم اتزان الجهد ومعامل القدرة.
- [ ] **لوحة استهلاك الطاقة:** إنشاء واجهة لتحليل استهلاك الطاقة والفقد في الشبكة.
- [ ] **لوحة الأمان:** إنشاء واجهة لمراقبة الكاميرات وسجل الدخول وأحداث الأمان.

#### 2.3. تحسينات عامة
- [ ] **تفاصيل المحطة:** إنشاء صفحة لعرض التفاصيل الكاملة لمحطة واحدة عند النقر عليها.
- [ ] **التحكم التفاعلي:** إضافة أزرار لإرسال أوامر تحكم (فصل/وصل) من واجهة المستخدم.
- [ ] **إقرار التنبيهات:** تفعيل أزرار الإقرار والمسح في قائمة التنبيهات لتعمل بشكل صحيح.

---

### 3. مهام إضافية

- [ ] **توثيق API:** إنشاء توثيق Swagger/OpenAPI لجميع الـ Endpoints.
- [ ] **الاختبارات (Testing):** كتابة اختبارات الوحدات (Unit Tests) واختبارات التكامل (Integration Tests) للـ Backend.
- [ ] **النشر (Deployment):** إعداد ملفات Docker و Docker Compose لنشر النظام بالكامل.
